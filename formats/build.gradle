plugins {
    id 'java-library'
}

dependencies {
    api project(':cache:codec')
    implementation group: 'de.javagl', name: 'jgltf-model', version: jgltfVersion
}

license {
    header = file(rootDir.path + '\\license\\LGPL.txt')
}